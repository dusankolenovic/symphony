Dark, translucent text field with an indigo focus ring — the Symphony contact form style. Pass `placeholder` and optionally `label`.

```jsx
<Input placeholder="Work email" type="email" />
<Input label="Company name" placeholder="Acme Inc." />
```
