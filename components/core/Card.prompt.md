Surface container. `variant="glass"` for translucent panels floating over the aurora gradient (Vision/Mission); `variant="solid"` for dark bordered cards on the near-black canvas.

```jsx
<Card variant="glass"><h3>Our Vision</h3><p>…</p></Card>
<Card variant="solid">…</Card>
```
