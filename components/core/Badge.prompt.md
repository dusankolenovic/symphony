Tiny pill label. Use `tone="new"` for solid-indigo "NEW" flags on announcements, `tone="stat"` for glassy monospace metric chips on cards (50%, $4-6M).

```jsx
<Badge tone="new">New</Badge>
<Badge tone="stat">50%</Badge>
```
