The official Symphony wordmark — lowercase "symphony" with the signature diagonal-stripe texture, in brand purple. Renders the packaged logo image.

```jsx
<Wordmark height={28} />
```

Point `src` at your own path if the asset lives elsewhere. The mark is purple-on-transparent, so it reads on both dark and light backgrounds.
