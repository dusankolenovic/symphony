Primary site header: wordmark on the left, glassy pill navigation on the right (some items carry a dropdown caret). Defaults to the real Symphony menu.

```jsx
<NavBar active="Who we are" onSelect={setPage} />
```
