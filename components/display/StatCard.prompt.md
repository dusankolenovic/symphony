Value / metric card with a square glyph tile, a monospace stat chip, and a title + subline. Used in outcome grids.

```jsx
<StatCard icon={<TrendIcon/>} stat="50%" title="Modernize your data layer" sub="~50% faster implementation" />
```
