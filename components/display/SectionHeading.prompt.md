Symphony's signature two-tone headline — white lead text with the emphasis word in periwinkle. Use `size="xl"` + `as="h1"` for hero.

```jsx
<SectionHeading as="h1" size="xl" lead="We are" accent="Symphony" />
<SectionHeading lead="Global" accent="footprint" />
```
